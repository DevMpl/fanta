
<h1 class="ui header"><i class="tag icon"></i><div class="content">項目設計</div></h1>

  <div class="ui breadcrumb">
    <div class="section"><%= link_to raw('<i class="home icon"></i>'), fanta_root_path, class: 'section' %></div>
    <i class="right chevron icon divider"></i>
    <%= link_to("項目設計", projects_path) %>
    <i class="right chevron icon divider"></i>項目設計 新規登録
  </div>

<div class="ui hidden divider"></div>

<%= render :partial => 'sub_menu' %>

<div class="ui warning message">
  <abbr title="required">*</abbr>は必須入力項目です。全てご記入いただかないと登録できませんのでご注意ください。
</div>

<%= simple_form_for(@project, :url => projects_path) do |f| %>
	<%= render partial: 'form', locals: { f: f } %>
<% end %>